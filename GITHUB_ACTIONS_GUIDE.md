# 🤖 Автоматическая сборка через GitHub Actions

GitHub Actions автоматически собирает оба .exe файла при каждом push в репозиторий!

## 📦 Как получить собранные .exe файлы

### Способ 1: Скачать артефакты из Actions (рекомендуется)

1. Откройте ваш репозиторий на GitHub: `https://github.com/peklos/CarShareX`

2. Перейдите во вкладку **Actions** (верхнее меню)

3. Найдите последний успешный запуск workflow **"Build CarShareX Desktop Apps"** ✅

4. Прокрутите вниз до секции **Artifacts**

5. Скачайте любой из артефактов:
   - **CarShareX-Desktop-Complete** - 📦 полный пакет (оба .exe + инструкция)
   - **CarShareX-Backend-Windows** - только Backend API
   - **CarShareX-Frontend-Windows** - только Frontend Desktop

### Способ 2: Запустить сборку вручную

1. Перейдите в **Actions** → **Build CarShareX Desktop Apps**

2. Нажмите **Run workflow** (справа)

3. Выберите ветку (например, `claude/csharp-app-to-backend-01E13TwHvwi2WYq8nb89uA5B`)

4. Нажмите зеленую кнопку **Run workflow**

5. Дождитесь завершения (~10-15 минут)

6. Скачайте артефакты как описано выше

## 📋 Что находится в артефактах

### 🎁 CarShareX-Desktop-Complete (полный пакет)
```
CarShareX-Desktop-Complete.zip
├── CarShareX-Backend.exe    # Backend API сервер
├── CarShareX-Frontend.exe   # Desktop приложение
└── README.txt               # Инструкция по запуску
```

### ⚙️ CarShareX-Backend-Windows
```
CarShareX-Backend.zip
└── CarShareX-Backend.exe    # Только backend
```

### 🖥️ CarShareX-Frontend-Windows
```
CarShareX-Frontend.zip
└── CarShareX-Frontend.exe   # Только frontend
```

## 🚀 Как запустить собранные приложения

После скачивания и распаковки:

1. **Запустите Backend:**
   ```
   Двойной клик на CarShareX-Backend.exe
   Должно появиться окно консоли с надписью:
   "✅ CarShareX Backend API запущен"
   "🌐 API URL: http://localhost:5000"
   ```
   ⚠️ **НЕ ЗАКРЫВАЙТЕ ЭТО ОКНО!**

2. **Запустите Frontend:**
   ```
   Двойной клик на CarShareX-Frontend.exe
   Откроется окно desktop приложения
   ```

3. **Готово!** Можете пользоваться приложением

## 🔑 Тестовые учетные данные

**Клиент:**
- Email: `ivan@mail.ru`
- Пароль: `password123`

**Администратор:**
- Email: `admin@carsharex.ru`
- Пароль: `admin123`

## 📊 Swagger API

После запуска backend откройте в браузере: `http://localhost:5000/swagger`

## 🗄️ База данных

SQLite БД автоматически создается в:
```
%APPDATA%\CarShareX\carsharex.db
```

На Windows это обычно:
```
C:\Users\<ВашеИмя>\AppData\Roaming\CarShareX\carsharex.db
```

## 🔍 Troubleshooting

### Workflow не запускается
- Проверьте, что файл `.github/workflows/build-executables.yml` существует
- Убедитесь, что Actions включены в настройках репозитория: Settings → Actions → General

### Сборка падает с ошибкой
- Проверьте логи в Actions (кликните на красный ❌ workflow)
- Убедитесь, что все зависимости корректны в `package.json` и `.csproj`

### Не могу скачать артефакты
- Артефакты доступны только авторизованным пользователям GitHub
- Войдите в свой аккаунт GitHub
- Артефакты хранятся 90 дней по умолчанию

## ⏱️ Время сборки

Примерное время сборки на GitHub Actions:
- **Backend (C#):** ~3-5 минут
- **Frontend (Tauri):** ~10-15 минут
- **Итого:** ~15-20 минут

## 🔧 Настройка workflow

Workflow автоматически запускается при:
- ✅ Push в ветку `main`
- ✅ Push в любую ветку `claude/**`
- ✅ Создании Pull Request в `main`
- ✅ Ручном запуске через UI

Конфигурация: `.github/workflows/build-executables.yml`

## 📝 Дополнительная информация

Для локальной сборки без GitHub Actions смотрите: [BUILD_AND_RUN.md](BUILD_AND_RUN.md)
